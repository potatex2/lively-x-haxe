package classes;

// global finals, more soon

/**Storage class for variables and paths; set to final for now until overriding for customization is needed.*/
final class Const {
    /**
        Root directory for files. *For now...*
    */
    public static final RD:String = "bulkAssets/";
}
 